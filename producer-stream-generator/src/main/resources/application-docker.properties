# =============================================================================
# VitalWatch - Stream Generator Producer (Docker Profile)
# =============================================================================

# Kafka Configuration (Docker network)
kafka.bootstrap-servers=kafka1:9092,kafka2:9092,kafka3:9092

# Stream Generation
stream.generation.enabled=${STREAM_GENERATION_ENABLED:true}
stream.interval.ms=${STREAM_INTERVAL_MS:1000}
