# 🏗️ Arquitectura del Sistema - VitalWatch

## 📋 Índice

1. [Visión General](#visión-general)
2. [Arquitectura de Componentes](#arquitectura-de-componentes)
3. [Diagrama de Despliegue](#diagrama-de-despliegue)
4. [Modelo de Datos](#modelo-de-datos)
5. [Flujos de Proceso](#flujos-de-proceso)
6. [Seguridad](#seguridad)

---

## 🎯 Visión General

VitalWatch es un sistema Cloud Native basado en microservicios para monitoreo hospitalario en tiempo real.

### Características Principales

- **Escalabilidad**: Arquitectura basada en contenedores
- **Resiliencia**: Health checks y reintentos automáticos
- **Seguridad**: Autenticación JWT y validación de datos
- **Monitoreo**: Logging centralizado y métricas

---

## 🏛️ Arquitectura de Componentes

### Diagrama de Alto Nivel

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CAPA DE PRESENTACIÓN                       │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Angular Frontend (SPA)                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │  │
│  │  │Dashboard │  │Pacientes │  │  Signos  │  │ Alertas  │     │  │
│  │  │ Module   │  │  Module  │  │  Vitales │  │  Module  │     │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │  │
│  │                                                                 │  │
│  │  ┌──────────────────────────────────────────────────────────┐ │  │
│  │  │           Core Services (Auth, API, Guards)              │ │  │
│  │  └──────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│                          Port: 4200 (Nginx)                          │
└───────────────────────────────┬───────────────────────────────────┘
                                │ HTTP/REST
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          CAPA DE API GATEWAY                         │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                      Kong API Gateway                          │  │
│  │                                                                 │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │  │
│  │  │   CORS   │  │   Rate   │  │ Security │  │ Logging  │     │  │
│  │  │  Plugin  │  │ Limiting │  │ Headers  │  │  Plugin  │     │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │  │
│  │                                                                 │  │
│  │  ┌──────────────────────────────────────────────────────────┐ │  │
│  │  │              Load Balancer & Health Checks               │ │  │
│  │  └──────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│                          Port: 8000                                  │
└───────────────────────────────┬───────────────────────────────────┘
                                │ HTTP/REST
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          CAPA DE APLICACIÓN                          │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                 Spring Boot Backend (REST API)                 │  │
│  │                                                                 │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │                  REST Controllers                       │  │  │
│  │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │  │  │
│  │  │  │ Auth │ │Pacien│ │Signos│ │Alert │ │Dashb │        │  │  │
│  │  │  │      │ │ tes  │ │Vital │ │ as   │ │oard  │        │  │  │
│  │  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘        │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                           ▼                                    │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │                 Service Layer (Business Logic)          │  │  │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │  │  │
│  │  │  │ AuthService  │  │PacienteServ. │  │AlertaService │ │  │  │
│  │  │  └──────────────┘  └──────────────┘  └──────────────┘ │  │  │
│  │  │  ┌──────────────┐  ┌──────────────┐                   │  │  │
│  │  │  │SignosVitServ.│  │DashboardServ.│                   │  │  │
│  │  │  └──────────────┘  └──────────────┘                   │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                           ▼                                    │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │            Repository Layer (Data Access - JPA)         │  │  │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │  │  │
│  │  │  │PacienteRepo  │  │SignosVitRepo │  │ AlertaRepo   │ │  │  │
│  │  │  └──────────────┘  └──────────────┘  └──────────────┘ │  │  │
│  │  │  ┌──────────────┐                                      │  │  │
│  │  │  │ UsuarioRepo  │                                      │  │  │
│  │  │  └──────────────┘                                      │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                                                                 │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │                  Cross-Cutting Concerns                 │  │  │
│  │  │  • Exception Handling (GlobalExceptionHandler)          │  │  │
│  │  │  • Validation (Bean Validation)                         │  │  │
│  │  │  • Security (Spring Security)                           │  │  │
│  │  │  • API Documentation (OpenAPI/Swagger)                  │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│                          Port: 8080                                  │
└───────────────────────────────┬───────────────────────────────────┘
                                │ JDBC/JPA
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          CAPA DE DATOS                               │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │           Oracle Cloud Autonomous Database (19c)               │  │
│  │                                                                 │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │  │
│  │  │PACIENTES │  │  SIGNOS  │  │ ALERTAS  │  │ USUARIOS │     │  │
│  │  │          │  │ VITALES  │  │          │  │          │     │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │  │
│  │                                                                 │  │
│  │  Features:                                                      │  │
│  │  • Auto-scaling                                                 │  │
│  │  • Automatic Backups                                            │  │
│  │  • High Availability                                            │  │
│  │  • Encryption at Rest                                           │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│                    Service: s58onuxcx4c1qxe9_high                   │
│                    Region: Santiago, Chile                           │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Diagrama de Despliegue

### Arquitectura Docker

```
┌─────────────────────────────────────────────────────────────────┐
│                         Docker Host                              │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Docker Network: vitalwatch-network          │   │
│  │                                                           │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  Container: vitalwatch-frontend                   │   │   │
│  │  │  ┌────────────────────────────────────────────┐  │   │   │
│  │  │  │  Nginx:alpine                               │  │   │   │
│  │  │  │  - Serve Angular SPA                        │  │   │   │
│  │  │  │  - Reverse Proxy to Backend                 │  │   │   │
│  │  │  │  Port: 4200:80                              │  │   │   │
│  │  │  └────────────────────────────────────────────┘  │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                        │                                  │   │
│  │                        ▼                                  │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  Container: vitalwatch-api-gateway               │   │   │
│  │  │  ┌────────────────────────────────────────────┐  │   │   │
│  │  │  │  Kong:3.4                                   │  │   │   │
│  │  │  │  - API Gateway                              │  │   │   │
│  │  │  │  - Rate Limiting                            │  │   │   │
│  │  │  │  - CORS                                     │  │   │   │
│  │  │  │  Port: 8000:8000                            │  │   │   │
│  │  │  └────────────────────────────────────────────┘  │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                        │                                  │   │
│  │                        ▼                                  │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  Container: vitalwatch-backend                    │   │   │
│  │  │  ┌────────────────────────────────────────────┐  │   │   │
│  │  │  │  eclipse-temurin:17-jre                     │  │   │   │
│  │  │  │  - Spring Boot Application                  │  │   │   │
│  │  │  │  - REST API                                 │  │   │   │
│  │  │  │  - Business Logic                           │  │   │   │
│  │  │  │  Port: 8080:8080                            │  │   │   │
│  │  │  │  Volume: ./Wallet_* (Oracle Wallet)         │  │   │   │
│  │  │  └────────────────────────────────────────────┘  │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼ JDBC/TCPS                         │
└──────────────────────────────┼───────────────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  Oracle Cloud        │
                    │  Autonomous Database │
                    │  (External Service)  │
                    └──────────────────────┘
```

### Volúmenes y Configuración

```
Volumes:
├── ./Wallet_S58ONUXCX4C1QXE9:/app/wallet:ro  (Backend)
├── ./backend/target:/app/target               (Backend - Build artifacts)
└── ./frontend/dist:/usr/share/nginx/html      (Frontend - Static files)

Environment Variables:
├── SPRING_PROFILES_ACTIVE=dev
├── ORACLE_DB_PASSWORD=$-123.Sb-123
├── JAVA_TOOL_OPTIONS=-Djavax.net.ssl.trustStore=...
└── API_URL=http://localhost:8080/api/v1
```

---

## 💾 Modelo de Datos

### Diagrama Entidad-Relación

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUARIOS                                 │
├─────────────────────────────────────────────────────────────────┤
│ PK │ id              BIGINT                                      │
│    │ nombre          VARCHAR(100)                                │
│    │ email           VARCHAR(100) UNIQUE                         │
│    │ password_hash   VARCHAR(255)                                │
│    │ rol             VARCHAR(20) [ADMIN, MEDICO, ENFERMERA]      │
│    │ activo          BOOLEAN                                     │
│    │ fecha_creacion  TIMESTAMP                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         PACIENTES                                │
├─────────────────────────────────────────────────────────────────┤
│ PK │ id                  BIGINT                                  │
│    │ nombre              VARCHAR(100)                            │
│    │ apellido            VARCHAR(100)                            │
│    │ rut                 VARCHAR(12) UNIQUE                      │
│    │ fecha_nacimiento    DATE                                    │
│    │ edad                INTEGER                                 │
│    │ genero              CHAR(1) [M, F, O]                       │
│    │ sala                VARCHAR(20)                             │
│    │ cama                VARCHAR(10)                             │
│    │ estado              VARCHAR(20) [ESTABLE, MODERADO,         │
│    │                                  CRÍTICO, RECUPERACIÓN]     │
│    │ diagnostico         VARCHAR(500)                            │
│    │ fecha_ingreso       TIMESTAMP                               │
│    │ fecha_alta          TIMESTAMP                               │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ 1
                                │
                                │ N
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SIGNOS_VITALES                              │
├─────────────────────────────────────────────────────────────────┤
│ PK │ id                       BIGINT                             │
│ FK │ paciente_id              BIGINT → PACIENTES(id)             │
│    │ frecuencia_cardiaca      INTEGER                            │
│    │ presion_sistolica        INTEGER                            │
│    │ presion_diastolica       INTEGER                            │
│    │ temperatura              DECIMAL(4,2)                       │
│    │ saturacion_oxigeno       INTEGER                            │
│    │ frecuencia_respiratoria  INTEGER                            │
│    │ estado_conciencia        VARCHAR(20) [ALERTA, VERBAL,       │
│    │                                       DOLOR, INCONSCIENTE]  │
│    │ observaciones            VARCHAR(500)                       │
│    │ registrado_por           VARCHAR(100)                       │
│    │ fecha_registro           TIMESTAMP DEFAULT CURRENT_TIMESTAMP│
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ 1
                                │
                                │ N (trigger automático)
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                          ALERTAS                                 │
├─────────────────────────────────────────────────────────────────┤
│ PK │ id                   BIGINT                                 │
│ FK │ paciente_id          BIGINT → PACIENTES(id)                 │
│    │ tipo                 VARCHAR(50)                            │
│    │ mensaje              VARCHAR(500)                           │
│    │ severidad            VARCHAR(20) [BAJA, MODERADA, CRÍTICA]  │
│    │ estado               VARCHAR(20) [ACTIVA, RESUELTA,         │
│    │                                   DESCARTADA]               │
│    │ fecha_creacion       TIMESTAMP DEFAULT CURRENT_TIMESTAMP    │
│    │ fecha_resolucion     TIMESTAMP                              │
│    │ resuelto_por         VARCHAR(100)                           │
│    │ notas_resolucion     VARCHAR(500)                           │
└─────────────────────────────────────────────────────────────────┘
```

### Índices y Constraints

```sql
-- Índices para optimización
CREATE INDEX idx_pacientes_estado ON PACIENTES(estado);
CREATE INDEX idx_pacientes_sala ON PACIENTES(sala);
CREATE INDEX idx_signos_paciente_fecha ON SIGNOS_VITALES(paciente_id, fecha_registro DESC);
CREATE INDEX idx_alertas_paciente_estado ON ALERTAS(paciente_id, estado);
CREATE INDEX idx_alertas_severidad_estado ON ALERTAS(severidad, estado);

-- Constraints de integridad
ALTER TABLE SIGNOS_VITALES ADD CONSTRAINT fk_signos_paciente 
    FOREIGN KEY (paciente_id) REFERENCES PACIENTES(id) ON DELETE CASCADE;

ALTER TABLE ALERTAS ADD CONSTRAINT fk_alertas_paciente 
    FOREIGN KEY (paciente_id) REFERENCES PACIENTES(id) ON DELETE CASCADE;

-- Checks de validación
ALTER TABLE PACIENTES ADD CONSTRAINT chk_edad 
    CHECK (edad >= 0 AND edad <= 120);

ALTER TABLE SIGNOS_VITALES ADD CONSTRAINT chk_temperatura 
    CHECK (temperatura >= 30.0 AND temperatura <= 45.0);
```

---

## 🔄 Flujos de Proceso

### 1. Flujo de Autenticación

```
Usuario                Frontend              Backend              Database
  │                      │                      │                    │
  │  1. Ingresa          │                      │                    │
  │  credenciales        │                      │                    │
  ├─────────────────────>│                      │                    │
  │                      │  2. POST /auth/login │                    │
  │                      ├─────────────────────>│                    │
  │                      │                      │  3. Query usuario  │
  │                      │                      ├───────────────────>│
  │                      │                      │<───────────────────┤
  │                      │                      │  4. Usuario + hash │
  │                      │                      │                    │
  │                      │                      │  5. Validar password
  │                      │                      │                    │
  │                      │  6. JWT Token +      │                    │
  │                      │     User Data        │                    │
  │                      │<─────────────────────┤                    │
  │  7. Redirigir a      │                      │                    │
  │     Dashboard        │                      │                    │
  │<─────────────────────┤                      │                    │
  │                      │                      │                    │
  │  8. Guardar token    │                      │                    │
  │     en localStorage  │                      │                    │
  │                      │                      │                    │
```

### 2. Flujo de Registro de Signos Vitales

```
Enfermera            Frontend              Backend              Database
  │                      │                      │                    │
  │  1. Selecciona       │                      │                    │
  │  paciente            │                      │                    │
  ├─────────────────────>│                      │                    │
  │                      │                      │                    │
  │  2. Ingresa signos   │                      │                    │
  │  vitales             │                      │                    │
  ├─────────────────────>│                      │                    │
  │                      │                      │                    │
  │  3. Valida campos    │                      │                    │
  │     (frontend)       │                      │                    │
  │                      │                      │                    │
  │                      │  4. POST /signos-    │                    │
  │                      │     vitales          │                    │
  │                      ├─────────────────────>│                    │
  │                      │                      │  5. Validar DTO    │
  │                      │                      │                    │
  │                      │                      │  6. INSERT signos  │
  │                      │                      ├───────────────────>│
  │                      │                      │<───────────────────┤
  │                      │                      │  7. ID generado    │
  │                      │                      │                    │
  │                      │                      │  8. Evaluar rangos │
  │                      │                      │     normales       │
  │                      │                      │                    │
  │                      │                      │  9. Si anormal:    │
  │                      │                      │     INSERT alerta  │
  │                      │                      ├───────────────────>│
  │                      │                      │<───────────────────┤
  │                      │                      │                    │
  │                      │  10. Response OK +   │                    │
  │                      │      Alerta generada │                    │
  │                      │<─────────────────────┤                    │
  │  11. Mensaje éxito   │                      │                    │
  │<─────────────────────┤                      │                    │
  │                      │                      │                    │
```

### 3. Flujo de Generación Automática de Alertas

```
Lógica de Negocio (Backend)
│
├─ 1. Recibir signos vitales
│
├─ 2. Evaluar cada parámetro:
│   │
│   ├─ Frecuencia Cardíaca
│   │   ├─ < 60 bpm → CRÍTICA: Bradicardia
│   │   └─ > 100 bpm → CRÍTICA: Taquicardia
│   │
│   ├─ Presión Arterial
│   │   ├─ Sistólica > 140 → MODERADA: Hipertensión
│   │   └─ Sistólica < 90 → CRÍTICA: Hipotensión
│   │
│   ├─ Temperatura
│   │   ├─ > 38°C → MODERADA: Fiebre
│   │   └─ < 36°C → CRÍTICA: Hipotermia
│   │
│   ├─ Saturación O₂
│   │   └─ < 95% → CRÍTICA: Hipoxemia
│   │
│   └─ Estado de Conciencia
│       └─ INCONSCIENTE → CRÍTICA: Alteración conciencia
│
├─ 3. Si hay anormalidades:
│   │
│   ├─ Crear objeto AlertaDTO
│   │   ├─ tipo: "Signo Vital Anormal"
│   │   ├─ mensaje: Descripción detallada
│   │   ├─ severidad: BAJA | MODERADA | CRÍTICA
│   │   └─ estado: ACTIVA
│   │
│   └─ Guardar en BD
│
└─ 4. Retornar signos vitales + alertas generadas
```

---

## 🔒 Seguridad

### Capas de Seguridad

```
┌─────────────────────────────────────────────────────────────┐
│  Capa 1: Frontend                                            │
│  • Validación de formularios (Reactive Forms)                │
│  • Auth Guard (protección de rutas)                          │
│  • Token JWT en localStorage                                 │
│  • Interceptor HTTP (agregar token a requests)               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Capa 2: API Gateway (Kong)                                  │
│  • Rate Limiting (100 req/min)                               │
│  • CORS Policy                                               │
│  • Security Headers (X-Frame-Options, CSP)                   │
│  • Request Size Limiting (10MB)                              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Capa 3: Backend (Spring Security)                           │
│  • JWT Validation                                            │
│  • Bean Validation (@Valid, @NotNull, etc.)                  │
│  • SQL Injection Prevention (JPA/Prepared Statements)        │
│  • XSS Protection (Input Sanitization)                       │
│  • Exception Handling (no exponer stack traces)              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Capa 4: Base de Datos                                       │
│  • Encryption at Rest (Oracle Cloud)                         │
│  • TLS/SSL Connection (TCPS)                                 │
│  • Wallet Authentication                                     │
│  • Automatic Backups                                         │
│  • Audit Logging                                             │
└─────────────────────────────────────────────────────────────┘
```

### Autenticación JWT

```
Token Structure:
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "userId": 1,
    "email": "admin@vitalwatch.com",
    "rol": "ADMIN",
    "exp": 1706000000
  },
  "signature": "..."
}

Token Lifecycle:
1. Usuario hace login → Backend genera JWT
2. Frontend guarda JWT en localStorage
3. Cada request incluye: Authorization: Bearer <token>
4. Backend valida JWT en cada request
5. Si token expirado → 401 Unauthorized
6. Frontend redirige a login
```

---

## 📊 Métricas y Monitoreo

### Health Checks

```
Endpoint: GET /api/v1/health

Response:
{
  "status": "UP",
  "components": {
    "db": {
      "status": "UP",
      "details": {
        "database": "Oracle",
        "validationQuery": "SELECT 1 FROM DUAL"
      }
    },
    "diskSpace": {
      "status": "UP",
      "details": {
        "total": 500GB,
        "free": 250GB
      }
    }
  }
}
```

### Logging

```
Log Levels:
- ERROR: Errores críticos del sistema
- WARN: Advertencias (validaciones fallidas)
- INFO: Operaciones importantes (login, CRUD)
- DEBUG: Información de debugging

Log Format:
[timestamp] [level] [thread] [class] - message

Example:
[2026-01-23 19:52:46] [INFO] [http-nio-8080-exec-1] 
[PacienteController] - POST /api/v1/pacientes - Paciente creado: ID=1
```

---

## 🔄 Escalabilidad

### Estrategias de Escalado

```
Horizontal Scaling:
┌────────────┐
│ Frontend 1 │
└────────────┘
┌────────────┐     ┌──────────────┐     ┌────────────┐
│ Frontend 2 │────>│ Load Balancer│────>│  Backend 1 │
└────────────┘     └──────────────┘     └────────────┘
┌────────────┐                          ┌────────────┐
│ Frontend 3 │                          │  Backend 2 │
└────────────┘                          └────────────┘
                                        ┌────────────┐
                                        │  Backend 3 │
                                        └────────────┘
                                              │
                                              ▼
                                        ┌────────────┐
                                        │  Oracle DB │
                                        │ (Auto-scale)│
                                        └────────────┘
```

### Optimizaciones

- **Caching**: Redis para sesiones y datos frecuentes
- **CDN**: Distribución de assets estáticos
- **Database**: Connection pooling (HikariCP)
- **API**: Paginación en endpoints de listado
- **Frontend**: Lazy loading de módulos

---

**Última actualización**: 2026-01-23  
**Versión**: 1.0.0
